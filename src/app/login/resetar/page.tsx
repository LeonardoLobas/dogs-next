import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Resetar | Dogs",
    description: "Resete a sua senha",
};

export default async function PageResetar() {
    return (
        <div>
            <h1>PageResetar</h1>
        </div>
    );
}
